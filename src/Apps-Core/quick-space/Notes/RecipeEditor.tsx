import React from "react";
import { CustomFieldType } from "../../../Utils/Types";

const CERecipeEditor: React.FC<CustomFieldType> = ({ mode }) => {
  return <>Recipe editor{mode}</>;
};

export default CERecipeEditor;
