import React from "react";

const LayoutDesigner: React.FC<{ layout: []; onChange: (layout) => void }> = ({
  layout,
  onChange,
}) => {
  return <>LayoutDesigner</>;
};

export default LayoutDesigner;
